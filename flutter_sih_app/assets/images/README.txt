This folder is for app image assets.

Please place the provided logo image file here with the following path and name:

  assets/images/rail_chinh_logo.png

Use the exact filename and path so Flutter can load it via `Image.asset('assets/images/rail_chinh_logo.png')`.

After placing the file run:

  flutter pub get

Then re-run the app:

  flutter run
